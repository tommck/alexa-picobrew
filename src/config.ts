export const config = {
    appId: 'YOUR_APP_ID',
    debug: false,
    testServer: false, // TODO: how to do this?
    picobrew: {
        userId: "YOUR_USER_ID",
        auth: { // TODO: encrypt or something?
            user: "YOUR_USER_NAME",
            pass: "YOUR_PASSWORD"
        }
    }
};